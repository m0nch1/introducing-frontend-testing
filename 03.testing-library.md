# Testing Library を知る

Kent C. Dodds 氏のブログ [Introducing the React Testing Library](https://kentcdodds.com/blog/introducing-the-react-testing-library) にて説明されていますが、ここではもう少し噛み砕いて紹介してみようと思います。

## Testing Library の特徴

Testing Library は前章の Testing Trophy を提唱した Kent C. Dodds 氏が Testing Trophy の考え方を実践するために開発したテストライブラリです。

React 版のほか、Angular, Vue, Svelte 版もあります。

Testing Library がリリースされるまでは React のテストに [Enzyme](https://enzymejs.github.io/enzyme/) というライブラリが使われることが主流でした。
（公式ドキュメントには [Migrate from Enzyme](https://testing-library.com/docs/react-testing-library/migrate-from-enzyme/) というページもあります。）

Enzyme には以下のような機能が用意されていました。

- shallow レンダリング
  - A コンポーネントが、B コンポーネントを使用している場合に、A コンポーネントのテスト時に B コンポーネントはレンダリングせず（無視して）A コンポーネントにのみ着目してテストできる
- コンポーネントの内部状態やメソッドに直接アクセスできる
  - wrapper.state() でコンポーネントの内部状態にアクセスできる
  - wrapper.instance() でコンポーネントのインスタンスにアクセスでき、メソッドを直接呼び出せる

```tsx
// 例: enzyme の shallow レンダリングを使ったテストコード
import { shallow } from 'enzyme'
import { MyButton } from './MyButton'

test('登録すると送信済みになる', () => {
  const wrapper = shallow(<MyButton />)
  const instance = wrapper.instance() as { submitData: () => void }

  instance.submitData()

  expect(wrapper.state('isSubmitted')).toBe(true)
})
```

shallow レンダリングをすると、コンポーネントが結合して動作する時にバグが発生していても単体のテストは通ってうという信頼性の低いテストを生みやすくなってしまいます。
[Why I Never Use Shallow Rendering](https://kentcdodds.com/blog/why-i-never-use-shallow-rendering) では shallow レンダリングの問題点が詳しく説明されています。

また、前章で上げたように実装に依存したテストコードはリファクタリングしづらくなるという問題もあります。

### ユーザー中心のテスト

このドキュメントの表紙にも書いてありますが、、**テストの内容が実際のソフトウェアの使われ方に近ければ近いほど、そのテストはより確実な自信を与えてくれる。** という原則があります。

Testing Library はこの原則に基づき、要素の取得は class や id などのユーザーが気にしないものではなく、ラベルやロール、画面上のテキストなど、ユーザーが実際に画面を操作する時に認知できる方法で要素を取得でき、且つクリックや入力などの操作結果に対して検証することができるようになっています。

つまり Testing Library を使うことで、開発者は内部ロジックではなく、実際のユーザーインタフェースの挙動をテストすることに集中できます。

Enzyme の `shallow` / `mount` が返す `wrapper` は、**ブラウザの DOM ノードそのものではなく、React コンポーネントツリーをラップしたオブジェクト**であり、 `wrapper.instance()` でクラスコンポーネントのインスタンスを取り出したり、`wrapper.state()` で内部 state を読んだりといったコンポーネント内部の状態にアクセスができます。

一方 `testing-library` の `render` は、テスト環境（多くは JSDOM）の中に **実際の DOM ツリーを組み立て**ます。これにより、ユーザーが行うのと同じようにテキストやリンクやボタンを検索することができます。

先ほどのテストコード例を Testing Library で書くと、次のようなイメージです。

```tsx
// 例: ユーザーが実際に識別可能な情報で要素を取得し、ユーザーの操作と同じ行動をテストする
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { MyButton } from './MyButton'

describe('MyButton', () => {
  it('「登録する」を押すと送信済みの表示になる', async () => {
    const user = userEvent.setup()
    render(<MyButton />)

    await user.click(screen.getByRole('button', { name: '登録する' }))

    expect(screen.getByText('送信しました')).toBeInTheDocument()
  })
})
```

この書き方であれば、ボタンをクリックした時に実行される関数名がリファクタリングされてもテストは壊れません。

### クエリの優先順位

テストコードを書くとき、対象の要素をどう取得するかに推奨される順番があります。
[Queries: Priority](https://testing-library.com/docs/queries/about#priority) では次のように推奨されています。

#### 誰にとっても利用しやすいクエリ

視覚やマウスを使用して操作するユーザーだけでなく、支援技術を利用するユーザーの操作体験も考慮したクエリです。

1. **`getByRole`**
   - アクセシビリティツリーに公開されているすべての要素を取得するために使用できます。
   - nameオプションを使うことで、取得した要素をアクセシブルな名前で絞り込むことが可能です。基本的には、まずこの方法を検討すべきです。
   - これを使って取得できない要素はほとんどありません。もし取得できないのであれば、そのUIはアクセシビリティに問題がある可能性があります。
   - 最も一般的な使い方は、`getByRole('button', {name: /submit/i})`のように、nameオプションを組み合わせる形です。
2. **`getByLabelText`**
   - フォームフィールドに対して非常に有効なメソッドです。
   - ユーザーがウェブサイトのフォームを操作する際、ラベルテキストを頼りに要素を探すのと同じ挙動を再現できるため、フォームでは優先的に使用すべきです。
3. **`getByPlaceholderText`**
   - プレースホルダーはラベルの代わりにはなりません。
   - しかし、他に選択肢がない場合は、他の方法よりも適しています。
4. **`getByText`**
   - フォーム以外の場所では、テキストコンテンツがユーザーにとって要素を見つける主な手がかりとなります。
   - このメソッドは、div、span、pタグといった非対話型の要素を見つけるのに適しています。
5. **`getByDisplayValue`**
   - すでに値が入力されているフォーム要素を操作する際、現在の値に基づいて要素を取得するのに便利です。

```ts
// getByRole ------------------------------
// HTML: <button>送信</button>
const submitBtn = screen.getByRole('button', { name: /送信/i });
// HTML:
// <nav aria-labelledby="main-nav-title">
//   <h2 id="main-nav-title">メインメニュー</h2>
//   <ul>
//     <li><a href="/">ホーム</a></li>
//     <li><a href="/about">会社概要</a></li>
//   </ul>
// </nav>
const mainNav = screen.getByRole('navigation', { name: /メインメニュー/i });

// getByLabelText ------------------------------
// HTML: 
// <label for="username-input">ユーザー名</label>
// <input id="username-input" />
const input = screen.getByLabelText(/ユーザー名/i);

// getByPlaceholderText ------------------------------
// HTML: <input type="password" placeholder="パスワードを入力" />
// ※ type="password" の場合は role を持たない
// ※ 詳しくは https://github.com/w3c/aria/issues/935 を参照
const searchInput = screen.getByPlaceholderText(/パスワードを入力/i);

// getByText ------------------------------
// HTML: <div>保存しました</div>
const message = screen.getByText(/保存しました/i);

// getByDisplayValue ------------------------------
// HTML: <input value="m0nch1@example.com" />
const emailInput = screen.getByDisplayValue('m0nch1@example.com');

```

#### HTML5およびARIAに準拠したセマンティッククエリ

`alt` や `title` など、**HTML5 や ARIA に沿った属性** を使って要素を取ります。

公式では、これらの属性に対する**実際のユーザー体験はブラウザや支援技術によって大きく異なる**ことに注意、と記載されています。

- **`getByAltText`**
  - img、area、input、およびalt属性をサポートするカスタム要素など、altテキストが利用可能な要素であれば、これを使って該当要素を検索できます。
- **`getByTitle`**
  - title属性はスクリーンリーダーによって一貫して読み上げられるわけではなく、視覚的なユーザーにとってもデフォルトでは表示されません。

```ts
// getByAltText ------------------------------
// HTML: <img alt="プロフィール写真" src="..." />
const profileImg = screen.getByAltText(/プロフィール写真/i);

// getByTitle ------------------------------
// HTML: <span title="詳細情報">?</span>
const infoIcon = screen.getByTitle(/詳細情報/i);
```

#### テスト用の ID

- **`getByTestId`**
  - これらはユーザーからは見えず（読み上げもされず）、ロールやテキストで要素を特定できない場合、あるいはテキストが動的に変化するなどそれらを使うのが適切でない場合にのみ推奨されます。

```ts
// getByTestId ------------------------------
// HTML: <div data-testid="custom-chart-container">...</div>
// ※ div 要素は role を持たない
const chart = screen.getByTestId('custom-chart-container');
```

この優先度に沿ってテストを書いていくことで、実装にアクセシビリティのフィードバックを与えることができるのも良い点です。

### 暗黙のロールとアクセシブルネーム

クエリで一番優先とされている `getByRole` は HTML 要素の `role` 属性を使って要素を取得します。

この `role` 属性は HTML 要素に対して**暗黙的**に（つまり、自分が設定しなくてもすでに）設定されていることが多いです。
たとえば `button` 要素には `role="button"` が設定されています。

```html
<!-- role 属性を指定していないが、暗黙的に role="button" が設定されている -->
<button>
  <span>ログイン</span>
</button>
```

このように初期値として保持しているロールを暗黙のロールと呼びます。

詳しくは [MDN: ARIA の使用: ロール、ステート、プロパティ](https://developer.mozilla.org/ja/docs/Web/Accessibility/ARIA/Guides/Techniques) を参考にすると良いでしょう。

#### ロールと要素は 1:1 ではないことがある

HTML 要素が持つ `role` 属性は一対一ではないことがあります。
例えば、 input 要素には type 属性によって暗黙的に `role` 属性が変わるだけでなく、type 属性と暗黙のロール名が一緒にはならないことがあります。

```html
<!-- role は textbox -->
<input type="text">

<!-- role は checkbox -->
<input type="checkbox">

<!-- role は radio -->
<input type="radio">

<!-- role は spinbutton -->
<input type="number">
```

#### アクセシブルネームを使って要素を絞り込む

アクセシブルネームというのは、スクリーンリーダーなどの支援技術が認識する要素の名前です。
実際にスクリーンリーダーを起動して Web サイトを操作すると、アクセシブルネームを読み上げることが確認できます。

よく画像に alt 属性を指定することがあると思いますが、この alt 属性の値がアクセシブルネームになります。

```ts
// const UserProfile = ({ avatarUrl, userName }: { avatarUrl: string; userName: string }) => {
//   return (
//     <div className="profile-card">
//       <img src={avatarUrl} alt={`${userName}のプロフィール写真`} />
//       <h2>{userName}</h2>
//     </div>
//   );
// };

test('プロフィールの画像が正しく表示されること', () => {
  render(<UserProfile avatarUrl="/path/to/img.jpg" userName="m0nch1" />);

  // <img> は暗黙のロールとして 'img' を持つため、name オプションで alt の内容を指定できる
  const profileImgByRole = screen.getByRole('img', { name: /m0nch1のプロフィール写真/i });
  expect(profileImgByRole).toBeInTheDocument();
});
```

#### aria 属性値を使って要素を絞り込む

アクセシブルネームの他に aria 属性値を使って絞り込むこともあります。具体的には以下のようなケースです。

```ts
// const TabMenu = () => {
//   return (
//     <div role="tablist">
//       <button role="tab" aria-selected="true">基本情報</button>
//       <button role="tab" aria-selected="false">設定</button>
//     </div>
//   );
// };

test('現在選択されているタブを取得する', () => {
  render(<TabMenu />);

  // aria-selected="true" のタブを絞り込む
  const activeTab = screen.getByRole('tab', { 
    name: /基本情報/i, 
    selected: true 
  });

  expect(activeTab).toBeInTheDocument();
});

// const PageHeader = () => {
//   return (
//     <header>
//       <h1>ダッシュボード</h1>
//       <section>
//         <h2>売上統計</h2>
//         <p>今月の売上データです。</p>
//       </section>
//     </header>
//   );
// };

// h1 - h6 要素の role は全て heading になる
// 見出しレベルで特定したい場合は aria-level を参照することができる
test('見出しレベルを指定して取得する', () => {
  render(<PageHeader />);

  const mainTitle = screen.getByRole('heading', { 
    name: /ダッシュボード/i, 
    level: 1 
  });
  expect(mainTitle).toBeInTheDocument();

  const subTitle = screen.getByRole('heading', { 
    name: /売上統計/i, 
    level: 2 
  });
  expect(subTitle).toBeInTheDocument();
});
```

#### ロールとアクセシブルネームの確認方法

最初から HTML の role が暗黙的に何が設定されており、アクセシブルネームがなんなのか、またどうすればアクセシブルネームを設定できるのかを全て覚えるのは難しいと思います。
そこで、ロールとアクセシブルネームを簡単に確認する方法を紹介します。

##### ブラウザの開発者ツールを使って確認する

![screen-shot-dev-tools-a11y-computed](/screen-shot-dev-tools-a11y-computed.png)

開発者ツールを使って対象の要素を選択すると Accessibility タブに Computed という項目があり、そこに role やアクセシブルネームなどが表示されます。

##### テストコードで確認する

Testing Library には [LogRoles](https://testing-library.com/docs/dom-testing-library/api-debugging/#logroles) というヘルパーがあります。これを使うと、テスト実行時にロールとアクセシブルネームをコンソールに出力して確認することができます。

```ts
import { render, logRoles } from '@testing-library/react';
import { MyComponent } from './MyComponent';

test('現在のロールとアクセシブルネームを一覧表示する', () => {
  // 1. コンポーネントをレンダリングし、container（DOMのルート）を取得
  const { container } = render(<MyComponent />);

  // 2. logRoles ヘルパーを呼び出す
  // 第1引数に DOM 要素、第2引数（オプション）にラベル文字列を渡せる
  logRoles(container);
});

// ログの出力例、name の部分がアクセシブルネーム
// Found 5 accessible roles:

// --------------------------------------------------
// navigation:
// Name "メイン":
// <nav aria-label="メイン" />
// --------------------------------------------------
// heading:
// Name "ログイン":
// <h1 />
// --------------------------------------------------
// textbox:
// Name "メールアドレス":
// <input id="email" type="text" />
// --------------------------------------------------
// checkbox:
// Name "利用規約に同意する":
// <input id="terms" type="checkbox" />
// --------------------------------------------------
// button:
// Name "送信":
// <button />
```

## まとめ

Testing Library は結合テスト中心にアプリケーションの信頼性が高いテストを書くことができるライブラリです。

また、単にバグを防ぐだけではなく、アクセシビリティのフィードバックを与えることができるということもわかったかと思います。
