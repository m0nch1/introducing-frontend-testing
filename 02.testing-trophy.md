# Testing Trophy とは何か

## テストの範囲（サイズ）の話

フロントエンドアプリケーションをテストしよう！と思った時にどんなふうにテストを書くことができるでしょうか？

仮に、EC サイトを開発していると仮定して、以下の機能が正しく動くかをテストしたいとしましょう。

- ユーザーが欲しいものを正常に購入できるかを検証したい
  - 商品一覧ページから欲しい商品詳細ページに遷移する
  - カートに商品を追加して、購入ページに遷移する
  - 購入ページで個人情報を入力し、決済を行う
  - 決済が完了したら購入完了ページに遷移する

```ts
import { test, expect } from '@playwright/test'

test('テスト商品をカートに入れ、決済完了すると決済完了ページが正しく表示される', async ({
  page,
}) => {
  // 商品一覧からテスト商品の詳細へ
  await page.goto('/products')
  await page.getByRole('link', { name: /テスト商品/ }).click()
  await expect(page).toHaveURL(/\/products\//)

  await page.getByRole('button', { name: 'カートに入れる' }).click()

  await page.getByRole('link', { name: 'カートを見る' }).click()
  await expect(page).toHaveURL(/\/cart/)

  await page.getByRole('link', { name: 'レジに進む' }).click()
  await expect(page).toHaveURL(/\/checkout/)

  await page.getByLabel('お名前').fill('山田太郎')
  await page.getByLabel('カード番号').fill('4242424242424242')
  await page.getByRole('button', { name: '支払う' }).click()

  // 決済完了ページの見出しと、発行された購入 ID
  await expect(
    page.getByRole('heading', { name: '決済が完了しました' }),
  ).toBeVisible()
  await expect(page.getByText(/購入ID[：:]\s+\S+/)).toBeVisible()
})
```

一連の操作は確認できていそうですが、次のような内容まで同じ雰囲気で書いていくとするとどうでしょうか。

- 複雑な送料の計算ロジックを検証したい
- カード番号のバリデーションは Visa / Mastercard などのブランドごとに違う。validateCard という関数でバリデーションをしており、それを網羅的に検証したい
- 定期便と通常商品を同じカートに入れられないなどのロジックがちゃんと動くか確認したい

こういった細かい分岐やケースがある内容を同じように書いていくと、テストコードが膨大な量になりそうですね。テストの実行時間が長くなりそうな感じがします。

## フロントエンドを、何を検証するかで切り出してみる

商品一覧ページを例に考えてみましょう。１つの画面を構成するのにも複数のモジュールが組み合わさっているはずです。

- API通信: サーバーから商品データを取得する（fetch）
- データ加工: 取得したデータを画面表示用に整形する
- 状態制御: ページネーション、検索、絞り込み、ソートを反映する
- UI表示: ユーザーが操作しやすい形でコンポーネントを描画する

これらすべてをアプリケーション全体として一度にテストしようとすると大変です。そこで、**「検証の目的」**に合わせて以下の3つの領域にテストを切り出します。

### ロジック

単体テスト / Unit テストと言われる領域

- 関数やモジュール単位に対し、「特定の入力に対して、期待通りの出力が返るか」をチェックする
- 依存関係を持たず、またはテストのために依存関係がダミーされたもの

```ts
// 例1: 金額表示を format する utility
import { describe, it, expect } from 'vitest'
import { formatPrice } from './utils/formatPrice'

describe('formatPrice', () => {
  it('円マークとカンマ区切りで返す', () => {
    expect(formatPrice(980)).toBe('¥980')
    expect(formatPrice(1234567)).toBe('¥1,234,567')
  })
})
```

```tsx
// 例2: 1 コンポーネントの「props → 表示」を網羅的に検証
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { OrderStatusBadge } from './OrderStatusBadge'

describe('OrderStatusBadge', () => {
  it.each([
    ['pending', '入金待ち'],
    ['shipped', '発送済み'],
    ['cancelled', 'キャンセル'],
  ] as const)('status が %s のとき「%s」と表示する', (status, label) => {
    render(<OrderStatusBadge status={status} />)
    expect(screen.getByText(label)).toBeInTheDocument()
  })
})
```

### 描画と振る舞い

結合テスト / Integration テストと言われる領域

- 複数の関数やモジュールが互いに連動して動作する様子をテストする
- 「表示内容とユーザー操作に対して意図どおり動くか」をチェックする
- ネットワークのリクエストは実際にせずにダミーのデータを使ってテストすることが多い

```tsx
// 例1: 複数のコンポーネントで構成される Dialog コンポーネントのテスト
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { QuantityDialog } from './QuantityDialog'

describe('QuantityDialog', () => {
  it('個数が 0 のときはバリデーションで「カートに入れる」が無効化される', async () => {
    const user = userEvent.setup()
    const onConfirm = vi.fn()

    render(
      <QuantityDialog
        title="カートに入れる個数"
        initialQuantity={1}
        open
        onConfirm={onConfirm}
        onClose={() => {}}
      />,
    )

    await user.click(screen.getByRole('button', { name: '減らす' }))
    expect(screen.getByRole('spinbutton', { name: '個数' })).toHaveValue(0)

    const confirm = screen.getByRole('button', { name: 'カートに入れる' })
    expect(confirm).toBeDisabled()
    expect(onConfirm).not.toHaveBeenCalled()
  })
})
```

```tsx
// 例2: 個人情報の保存に成功した場合と失敗した場合でメッセージが切り替わる
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { SaveProfileForm } from './SaveProfileForm'

describe('SaveProfileForm', () => {
  beforeEach(() => {
    vi.stubGlobal('fetch', vi.fn())
  })

  it('保存に成功すると完了メッセージを出す', async () => {
    vi.mocked(fetch).mockResolvedValue(
      new Response(JSON.stringify({ ok: true }), {
        status: 200,
        headers: { 'Content-Type': 'application/json' },
      }),
    )
    const user = userEvent.setup()
    render(<SaveProfileForm />)

    await user.click(screen.getByRole('button', { name: '保存' }))

    await waitFor(() => {
      expect(screen.getByText('保存しました')).toBeInTheDocument()
    })
  })

  it('保存が失敗するとエラーメッセージを出す', async () => {
    vi.mocked(fetch).mockResolvedValue(
      new Response(JSON.stringify({ error: 'server' }), {
        status: 500,
        headers: { 'Content-Type': 'application/json' },
      }),
    )
    const user = userEvent.setup()
    render(<SaveProfileForm />)

    await user.click(screen.getByRole('button', { name: '保存' }))

    await waitFor(() => {
      expect(screen.getByText(/保存に失敗しました/)).toBeInTheDocument()
    })
  })
})
```

### ユーザーフロー

E2E テストと言われる領域

- 最もユーザーの操作に近い環境でユーザーが目的を達成できるかをチェックする
- ネットワークリクエストもできる限りモックせずに検証する

```ts
// 例: 購入フローの代表的なケースを検証
import { test, expect } from '@playwright/test'

test('カートに入れた商品が問題なく注文できる', async ({
  page,
}) => {
  await page.goto('/products')
  await page.getByRole('link', { name: /テスト商品/ }).click()
  await expect(page).toHaveURL(/\/products\//)
  await page.getByRole('button', { name: 'カートに入れる' }).click()
  await page.getByRole('link', { name: 'カートを見る' }).click()
  await expect(page).toHaveURL(/\/cart/)
  await page.getByRole('link', { name: 'レジに進む' }).click()
  await expect(page).toHaveURL(/\/checkout/)
  await page.getByLabel('お名前').fill('山田太郎')
  await page.getByLabel('カード番号').fill('4242424242424242')
  await page.getByRole('button', { name: '支払う' }).click()

  await expect(
    page.getByRole('heading', { name: '決済が完了しました' }),
  ).toBeVisible()
  await expect(page.getByText(/購入ID[：:]\s+\S+/)).toBeVisible()
})
```

## テスト戦略の歴史

３つに分けた領域をおさらいしておきましょう。

| テストの種類 | 説明 |
| --- | --- |
| 単体テスト / Unit テスト | 関数やモジュール単位のレベル |
| 結合テスト / Integration テスト | 複数の関数やモジュールが組み合わさったレベル |
| E2E テスト | アプリケーション全体のレベル。ほぼ手動のテストをコード化したもの |

フロントエンドのテストと一口に行ってもテストの手法によってカバーできる領域が異なることがわかりました。

このテストの手法はどのように使い分ければいいのでしょうか？

### テストピラミッド

ここでやっとテスト戦略モデルというものを紹介します。

これは Mike Cohn 氏が [Succeeding with Agile](https://www.amazon.com/Succeeding-Agile-Development-Addison-Wesley-Signature-ebook/dp/B002TIOYWQ) で提案したテスト戦略モデルです。

![テストピラミッド](/test-pyramid.png)

この当時、アジャイル開発におけるテストの自動化の主流は画面操作を記録・再生するような E2E テストでしたが、実行が遅いというのと、**些細な画面変更でテストが壊れてしまう**という問題がありました。

これは画像の左図の状態であり、アイスクリームコーン型と言われるモデルです。

```ts
// 例: グローバルナビを「何番目」で指定している（項目追加・並べ替えで広範囲が壊れる）
import { test, expect } from '@playwright/test'

test('ヘッダーから商品一覧へ', async ({ page }) => {
  await page.goto('/')
  await page.getByRole('navigation').getByRole('link').nth(0).click()
  await expect(page).toHaveURL(/\/products/)
})

test('ヘッダーからカートへ', async ({ page }) => {
  await page.goto('/')
  await page.getByRole('navigation').getByRole('link').nth(2).click()
  await expect(page).toHaveURL(/\/cart/)
})
```

アイスクリームコーン型のようなテストでは「本質的なバグではない」理由でテストが失敗することが多く（fragility）、このような脆さにコストをかけないように、ロジックは UI を通さずにテストを書くことが重要だと考えられました。

テストピラミッド型のテスト戦略モデルに合わせてテストを書くことで、コスパよくリリースサイクルを上げつつ、壊れにくいテスト基盤を作れるようになりました。

## Testing Trophy を知る

Testing Trophy というテスト戦略モデルは [Testing Library](https://testing-library.com/) というテストライブラリの作者である Kent C. Dodds 氏が提案したものです。
（Testing Library については後ほど紹介します）

![testing trophy](/testing-trophy.png)

Kent 氏は Assert(js) Conf 2018（JavaScriptのテスト技術や品質向上、テストフレームワークに特化した技術カンファレンス） で [Write tests. Not too many. Mostly integration.](https://www.youtube.com/watch?v=1aWo7aTQ-9o) というタイトルで、テストを書くときには Integration テストを中心に書くべきだという講演を行いました。

ブログ記事でも公開されているためこちらを読むのをオススメしますが、少し要約して紹介します。

- [Write tests. Not too many. Mostly integration.](https://kentcdodds.com/blog/write-tests)

### 静的解析が土台になる

テストの目的をおさらいしますが、「実装したプログラムが意図通りに動いているかを確認すること」でした。つまり、バグを防ぐということです。

その手段のひとつとして、**TypeScript** や **ESLint** のような静的解析は、実行コストが低くて効きます。

Testing Trophy の図では **Static** という領域が追加されていますが、これがテストの土台として重要であると位置付けられています。

### Write tests. Not too many

「テストを書こう！書きすぎず」というのはどうゆうことなのでしょうか。

テストにはカバレッジという指標があり、いわばテストコードが実装をどれくらい網羅しているかという指標です。

このカバレッジを 100% にすることが絶対の正義でしょうか？テストの目的は「実装したプログラムが意図通りに動いているかを確認すること」です。

100% という数値を達成するためだけに不要なテストを書くと、カバレッジを 100% にするためだけの目的が違うテストが増えてしまいます。

テストも当然メンテナンスが必要ですから、大量のテストをメンテナンスし続けることになります。

#### 実装の詳細をテストしていないか？

カバレッジを上げるために書くテストの代表例として、**実装の詳細**に寄りすぎたテストを書いてしまうことがあります。

実装の詳細と言っているのは、ユーザーからは見えない開発者都合の実装のことです。

```tsx
// ❌ ユーザーはボタンを押すだけ。submitData という関数名はユーザーには関係がない。
import { shallow } from 'enzyme'
import MyButton from './MyButton'

test('「登録」ボタンをクリックすると submitData が呼ばれる', () => {
  const wrapper = shallow(<MyButton />)
  const instance = wrapper.instance()

  // submitData という関数名を実装で変えた場合、このテストも修正しないとテストが失敗する
  instance.submitData()

  expect(wrapper.state('isSubmitted')).toBe(true)
})
```

### Mostly integration

単体のテストがすべて pass しても、組み合わせた時の使い方が誤っていると、実際の画面では壊れることがあります。

この例は TypeScript の型チェックが効いていれば防げるものですね。

```tsx
// email を必須で受け取り toLowerCase するコンポーネント
const UserDisplay = ({ name, email }: { name: string; email: string }) => {
  return (
    <div>
      <p>Name: {name}</p>
      <p>Email: {email.toLowerCase()}</p>
    </div>
  )
}

// email を渡し忘れている（実行時は email が undefined で落ちうる）
const UserProfile = () => {
  const user = { name: 'm0nch1', email: 'm0nch1@example.com' }
  // ※ 型が効いていれば TypeScript で検知できる例
  return <UserDisplay name={user.name} />
}
```

```tsx
// ❌ UserDisplay だけを切り離すと「正しい props を渡せば」通ってしまう
import { render, screen } from '@testing-library/react'
import { expect, test } from 'vitest'
import { UserDisplay } from './UserDisplay'

test('UserDisplay 単体', () => {
  render(<UserDisplay name="test" email="TEST@EX.COM" />)
  expect(screen.getByText('test@ex.com')).toBeInTheDocument()
})
```

#### テストの信頼度と費用対効果

先述のテストピラミッドの **縦方向**には、**時間・金銭的コスト**に加えて **「そのテストがどれだけバグが発生していないと信頼できるか」** という観点が存在しています。

Kent 氏はこのように言っています。

> Testing is a measure of how confident we are that our code will do what we expect it to do. It’s a measure of how much we can trust our code.

和訳すると「テストの内容が実際のソフトウェアの使われ方に近ければ近いほど、そのテストはより確実な自信を与えてくれる。」

E2E テストは Unit テストよりコストも時間もかかりますが、信頼度は高く、アプリケーションが正しく動いているという確認を遥かに高めてくれるものだということを言っています。

この点を踏まえると、統合テストは、信頼性とスピード／コストのバランスが非常に優れており、最も厚く書くべき、という結論に至るわけです。

### まとめ

テストそれぞれの書き方についても [Static vs. Unit vs. Integration vs. E2E Tests](https://kentcdodds.com/blog/static-vs-unit-vs-integration-vs-e2e-tests) で紹介されているのでこちらを見ることをオススメします。

改めて testing trophy の図を見ながらおさらいしましょう。

![testing trophy](/testing-trophy.png)

- End to End: ユーザーのようにアプリ内を操作し、正しく動作することを確認する。機能テストまたは E2E テストとも呼ばれる。
- Integration: 複数のユニットが調和して動作することを確認する。
- Unit: 個々の部品が正しく動作することを確認する。
- Static: コードを書くときに静的解析によってタイプミスや入力ミスを検出する。

ブログ記事でも挙げられている重要な観点を要約します。

#### トレードオフを考える

- トロフィーの上に行けば行くほど、テストが失敗し、メンテナンスする可能性は高くなる
- 単体テストは依存関係のない小さなものをテストするか、依存関係をモック化する（＝ モック対象の大量のコードを数行に置き換える）

つまり、テストのレベルが下であればあるほどテスト対象のプロダクションのコード量は少なくなります。
E2E のテストが１つのテストでカバーできるコード行数を Unit テストでカバーする場合、より多くのテストを書くことになることは想像しやすいと思います。

加えて、実施にテストのレベルが下がるに連れ、そもそもテストが出来ない部分も出てきます。

#### Static

出来ないこと：静的解析ツールではビジネスロジックが正しいかを保証することはできない
向いていること：引数に数値を求める関数に文字列が渡されないかを保証できる

#### Unit

出来ないこと：単体テストでは、依存関係を持つモジュールが正しく連携していることを保証できない
向いていること：複雑な計算ロジックやオブジェクトの整形処理などのエッジケース・境界値のテストをするのに向いている

#### Integration

出来ないこと：バックエンドに正しいデータが渡されているかなどの保証はできない
向いていること：検索フォームの入力内容が URL と連動するような機能のエッジケース・複数の組み合わせ条件のテストがしたい場合 E2E よりもコスパ良くテストができる

#### E2E

出来ないこと：通常は本番の環境ではなくテスト専用の環境を用意して行うため、環境差異によるテストの失敗が発生しうる
向いていること：ブラウザ、ネットワーク、データベース、外部APIなどがすべて組み合わさった状態で、アプリケーションがユーザーに提供する UI/UX が壊れていないかテストできる

### 銀の弾丸ではない

どのテストのレベルにもそれぞれトレードオフがあることがわかりますね。

E2E テスト が失敗した場合、どのコードが問題を引き起こしたのかを特定するのが難しくなることが多いですが、その分、テスト結果に対する信頼性も高まります。

これは、テスト作成に十分な時間をかけられない場合に特に役立ちます。そもそもテストで問題を発見できなかった場合よりも、圧倒的に良いはずだからです。

結局のところ、このテスト戦略はテストする目的を達成するためにどうすれば効率的にできるだろうか？という考え方の１つです。

大事なのは、ソフトウェアを提供する（デプロイ）するときにソースコードがビジネス要件を満たしているか/バグが発生していないか、を確信を持てるかどうかであり、プロジェクト/ソフトウェアによってテスト戦略を柔軟に組み合わせることが必要です。
