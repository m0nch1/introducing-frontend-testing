# その他の専門的なテスト

次に、これまで触れてこなかったフロントエンドの専門的なテスト（アクセシビリティテスト と ビジュアルテスト）についても紹介します。

## はじめに

Chromatic 社の [Frontend Testing Guide](https://www.chromatic.com/frontend-testing-guide/) で紹介されている図を引用します。

![chromatic-testing-trophy](/chromatic-testing-trophy.png)

Unit テストの上位に、Visual と A11y という領域があると思いますが、この部分のテストについてです。

## アクセシビリティテスト

Testing Library では `getByRole` や `getByLabelText` などのクエリの優先度を意識したテストコードを書くことで、プロダクションコードのアクセシビリティを確認しながらテストコードを書くことができることを紹介しました。

なので、Testing Library を用いて正しいクエリの順序でテストコードを書いていれば、アクセシビリティのテストとして機能します。

### vitest-axe

より詳細にアクセシビリティのテストを行うためのテストライブラリとして [vitest-axe](https://github.com/nickcolley/vitest-axe) などが有用です。

```ts
import { createElement } from 'react'
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'
import { axe, toHaveNoViolations } from 'vitest-axe'
import { ProductCard } from './ProductCard'

expect.extend(toHaveNoViolations)

describe('ProductCard', () => {
  it('アクセシビリティ違反がないこと', async () => {
    const { container } = render(
      createElement(ProductCard, {
        title: '豆電球',
        price: 980,
        currency: 'JPY',
      }),
    )

    // vitest-axe の axe 関数を使う
    const results = await axe(container)

    // toHaveNoViolations というマッチャーで検証することができる
    expect(results).toHaveNoViolations()
  })
})
```

### Axe

[Axe](https://github.com/dequelabs/axe-core)（axe-core）は、DOM を解析してアクセシビリティ違反を検出する代表的なエンジンです。テスト実行時に組み込むほか、ブラウザ拡張などでも利用できます。

加えて、ESLint のアクセシビリティルール（例: [eslint-plugin-jsx-a11y](https://github.com/jsx-eslint/eslint-plugin-jsx-a11y)）を併用すると、コードを書く段階でも問題を抑えやすくなります。

### 手動での確認

アクセシビリティのテストはブラウザ・端末・スクリーンリーダーの組み合わせを変えて、手動で確認することもやはり必要です。
なぜなら、静的解析や単体テストではユーザーの操作に限りなく近いとは言えないからです。

これには時間がかかるので、先述の静的解析や単体テストの領域でできるだけ検証しておくことで手動での確認のコストを減らすことができるはずです。

手動での確認方法の一例については freee 社の [axe DevToolsを使用したアクセシビリティー・チェック](https://a11y-guidelines.freee.co.jp/explanations/axe.html) を参考にすると良いと思います。

## ビジュアルリグレッションテスト

フロントエンドのソフトウェアは、様々なユーザー環境に対して提供されるため、無数のバリエーションが存在します。
ブラウザも多数あるし、操作する端末の画面サイズも様々です。

それらに対して全てが正しいかをテストするのは困難というのは想像に難くないでしょう。

![visual-test](/visual-test.png)

ビジュアルリグレッションテストは、リグレッションテストの一種であり、ビジュアルの変更があった場合に、その変更が意図したものかを確認するテストです。

フロントエンドの成果物はほとんどが見た目を伴う UI コンポーネントなので、ビジュアルリグレッションテストが重要視されます。

<!-- markdownlint-disable-next-line MD033 -->
<video src="/frontend-testing-guide-video.mp4" autoplay muted loop playsinline controls width="100%"></video>

### どんな場面で使うべきか

一番想像しやすいのは共有の UI コンポーネントライブラリを開発しているときなどでしょう。

コンポーネントライブラリは汎用的であり、柔軟なインターフェースと見た目を備えていることが多いと思います。

見た目が破壊的なバグを持つリリースをしてしまい、それが複数のプロジェクトで適用されてしまうと影響箇所は膨大になってしまいます。これを防ぐために有用なテストです。

また、プロジェクトで CSS の大幅なリファクタリングやライブラリの大型アップデートを控えているときに、一時的にビジュアルリグレッションテストを導入することで、見た目の破壊的なバグを防ぎながらマイグレーションを進めることもできると思います。

#### Playwright でのビジュアルリグレッションテスト

ビジュアルリグレッションテストを行う方法はいくつかありますが、ここでは Playwright を例に紹介します。

Playwright では、`toHaveScreenshot` というマッチャーを使ってビジュアルリグレッションテストを行うことができます。

詳しくは公式ドキュメント [Visual comparisons](https://playwright.dev/docs/test-snapshots) を参照してください。

```ts
import { test, expect } from '@playwright/test';

test('example test', async ({ page }) => {
  await page.goto('https://playwright.dev');
  await expect(page).toHaveScreenshot();
});
```
